import { Box, styled } from "@mui/material";

const Cheese = styled(Box)({
    height: 10,
    width: '75%',
    backgroundColor: '#FFDB2A96',
    borderRadius: '10px',
    borderBottom: '3px solid #ECBD0D',
    marginTop: 7
})

export default Cheese;