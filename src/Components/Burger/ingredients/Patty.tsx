import { Box, styled } from "@mui/material";

const Patty = styled(Box)({
    height: 30,
    width: '70%',
    backgroundColor: '#603102',
    borderRadius: '10px',
    borderBottom: '10px solid #402408',
    marginTop: 7
})

export default Patty;