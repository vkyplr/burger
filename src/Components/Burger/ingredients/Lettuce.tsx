import { Box, styled } from "@mui/material";

const Lettuce = styled(Box)({
    height: 10,
    width: '75%',
    backgroundColor: '#80D041',
    borderRadius: '8px',
    borderBottom: '3px solid #0F6D12',
    marginTop: 7
})

export default Lettuce;